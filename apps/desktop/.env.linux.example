# Copy to .env.linux (Git ignored). Release settings for the Linux arm64 Desktop target.
# Packaging reads release settings only from this file; shell and system variables never override it.
#
# Linux artifacts are always unsigned, so no signing, notarization, or upload field applies: this
# file carries only the shared application identity and mandatory-update policy settings.
DSH_DESKTOP_APP_ID=com.deepseek.harness
DSH_DESKTOP_AUTO_UPDATE_ENV=test

# The selected mandatory-update origin is required, including unsigned and preparation builds.
DSH_DESKTOP_MANDATORY_UPDATE_TEST_ORIGIN=
DSH_DESKTOP_MANDATORY_UPDATE_PROD_ORIGIN=
# The test deployment requires nonempty allowedAuthOrigins; the production deployment rejects them.
# Set approved login origins for test builds; omit allowedAuthOrigins for production.
DSH_DESKTOP_MANDATORY_UPDATE_CONFIG='{"allowedAuthOrigins":["https://login.example.com"]}'

# Optional npm registry for the bundled dsh runtime install; defaults to https://registry.npmjs.org/.
# A local mirror such as https://registry.npmmirror.com speeds up repeated local packages.
# DSH_DESKTOP_NPM_REGISTRY=https://registry.npmmirror.com

# Debian package maintainer written into the .deb metadata; defaults to a placeholder identity.
# DSH_DESKTOP_LINUX_MAINTAINER=Your Name <you@example.com>
